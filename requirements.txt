# Flask web framework
Flask==2.3.3
Werkzeug==2.3.7

# Core scientific computing (minimal versions)
numpy>=1.24.0
pandas>=1.5.0

# Essential bioinformatics only (remove heavy packages)
biopython>=1.80
biopandas>=0.4.0

# PRELYM preparation agent dependencies
pdb2pqr>=3.6.2  # Now available via pip - enables full preparation agent
# pdbfixer>=1.8  # Only available via conda, not pip
# openmm>=7.7     # Only available via conda, not pip

# Molecular analysis dependencies
freesasa>=2.1.0
MDAnalysis>=2.4.0
# propkatraj>=1.1.0  # Conflicts with pdb2pqr propka requirements
mdtraj>=1.9.7

# Excel file handling
openpyxl>=3.0.10

# System dependencies
gunicorn>=20.1.0
requests>=2.25.0