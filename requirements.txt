# Flask web framework
Flask==2.3.3
Werkzeug==2.3.7

# Core scientific computing (minimal versions)
numpy>=1.24.0
pandas>=1.5.0

# Essential bioinformatics only (remove heavy packages)
biopython>=1.80
biopandas>=0.4.0

# Excel file handling
openpyxl>=3.0.10

# System dependencies
gunicorn>=20.1.0